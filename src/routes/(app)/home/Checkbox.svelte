<input type="checkbox" on:change />

<style lang="scss">
	input {
		appearance: none;
		width: 1.4rem;
		height: 1.4rem;
		border: 2px solid var(--bg);
		&:checked {
			background: var(--bg);
		}
	}
</style>
